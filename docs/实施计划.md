# 定制每日财经新闻 - 实施计划

## 📅 计划概述

**创建日期**: 2026年1月25日
**项目阶段**: Phase 1 - 自动化新闻抓取系统
**目标**: 从手动更新升级为半自动化/全自动化系统

---

## 🎯 Phase 1: 新闻自动抓取系统（优先级：P0）

### 1.1 新闻源规划

#### 国内主流财经媒体

**权威媒体**:
1. **新华社** - 财经频道
   - URL: `http://www.xinhuanet.com/finance/`
   - 更新频率: 实时
   - 覆盖领域: 政策、宏观经济、金融市场
   - RSS: `http://www.xinhuanet.com/fortune/news_fortune.xml`

2. **人民日报** - 财经版
   - URL: `http://finance.people.com.cn/`
   - 更新频率: 每日
   - 覆盖领域: 政策解读、经济数据

3. **经济日报** - 财经要闻
   - URL: `http://www.economicdaily.com.cn/`
   - 更新频率: 每日

**专业财经媒体**:
4. **财新网** - 金融频道
   - URL: `https://finance.caixin.com/`
   - 更新频率: 实时
   - 覆盖领域: 金融市场深度报道
   - 付费内容: 部分文章需要订阅

5. **第一财经**
   - URL: `https://www.yicai.com/`
   - 更新频率: 实时
   - 覆盖领域: A股、宏观经济

6. **21世纪经济报道**
   - URL: `https://www.21jingji.com/`
   - 更新频率: 实时
   - 覆盖领域: 金融市场、政策

7. **上海证券报**
   - URL: `https://www.cnstock.com/`
   - 更新频率: 实时

8. **证券时报**
   - URL: `https://www.stcn.com/`
   - 更新频率: 实时

#### 国际主流财经媒体

**美国**:
1. **Bloomberg** (彭博社)
   - URL: `https://www.bloomberg.com/markets`
   - 覆盖领域: 全球市场、美国经济、货币政策
   - 语言: 英文
   - 特色: 数据详实、更新极快
   - RSS: `https://feeds.bloomberg.com/markets/news.rss`

2. **Reuters** (路透社)
   - URL: `https://www.reuters.com/finance`
   - 覆盖领域: 全球金融、大宗商品、外汇
   - 语言: 英文
   - 特色: 中立客观、覆盖全面
   - RSS: `https://www.reutersagency.com/feed/`

3. **Wall Street Journal** (华尔街日报)
   - URL: `https://www.wsj.com/markets`
   - 覆盖领域: 美股、美国经济、政策
   - 语言: 英文
   - 付费内容: 需要订阅

4. **Financial Times** (金融时报)
   - URL: `https://www.ft.com/markets`
   - 覆盖领域: 欧洲市场、全球金融
   - 语言: 英文
   - 特色: 欧洲视角

5. **CNBC**
   - URL: `https://www.cnbc.com/`
   - 覆盖领域: 美股市场、交易日动态
   - 语言: 英文

**欧洲**:
6. **Financial News** (伦敦)
   - URL: `https://www.efinancialnews.com/`
   - 覆盖领域: 欧洲金融、投行业务

7. **Euroweek**
   - URL: `https://www.euroweek.com/`
   - 覆盖领域: 欧洲债券、资本市场

**亚太**:
8. **Nikkei Asia** (日经亚洲)
   - URL: `https://asia.nikkei.com/`
   - 覆盖领域: 亚洲市场、日本经济
   - 语言: 英文（有部分中文内容）

9. **South China Morning Post** (南华早报)
   - URL: `https://www.scmp.com/business`
   - 覆盖领域: 香港、中国、亚洲市场
   - 语言: 英文

10. **The Straits Times** (海峡时报)
    - URL: `https://www.straitstimes.com/business`
    - 覆盖领域: 新加坡、东南亚市场

#### 区域专业媒体

11. **FXStreet** (外汇专业)
    - URL: `https://www.fxstreet.com/`
    - 覆盖领域: 外汇市场、央行政策

12. **MarketWatch**
    - URL: `https://www.marketwatch.com/`
    - 覆盖领域: 美股、个人投资

---

### 1.2 技术架构设计

#### 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    新闻抓取系统                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐      ┌──────────────┐               │
│  │  新闻源配置   │      │  抓取调度器   │               │
│  │  (JSON/YAML) │─────▶│  (Cron Job)  │               │
│  └──────────────┘      └──────┬───────┘               │
│                               │                         │
│                               ▼                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │            网页抓取模块 (Scrapers)                 │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  • RSS Feed Reader                               │  │
│  │  • HTML Parser (BeautifulSoup)                   │  │
│  │  • API调用 (如媒体提供API)                       │  │
│  └──────────────────────┬───────────────────────────┘  │
│                         │                              │
│                         ▼                              │
│  ┌──────────────────────────────────────────────────┐  │
│  │           内容处理模块 (Processors)               │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  1. 文本提取与清洗                               │  │
│  │  2. 机器翻译 (英文→中文)                         │  │
│  │  3. 去重与质量检查                               │  │
│  │  4. 分类与标签                                   │  │
│  └──────────────────────┬───────────────────────────┘  │
│                         │                              │
│                         ▼                              │
│  ┌──────────────────────────────────────────────────┐  │
│  │            AI评论生成模块                         │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  • Claude API / GPT-4 API                       │  │
│  │  • Prompt Engineering                           │  │
│  │  • 评论质量控制                                 │  │
│  └──────────────────────┬───────────────────────────┘  │
│                         │                              │
│                         ▼                              │
│  ┌──────────────────────────────────────────────────┐  │
│  │            HTML生成模块                           │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  • Jinja2 / 模板引擎                             │  │
│  │  • 应用设计规范                                   │  │
│  │  • 生成最终HTML文件                              │  │
│  └──────────────────────┬───────────────────────────┘  │
│                         │                              │
│                         ▼                              │
│  ┌──────────────────────────────────────────────────┐  │
│  │            质量检查与发布                         │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  • 内容审核                                      │  │
│  │  • 链接检查                                      │  │
│  │  • Git提交 / 文件保存                            │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 技术栈选择

**Python 3.10+**

```python
# 依赖库
requests          # HTTP请求
beautifulsoup4    # HTML解析
feedparser        # RSS解析
python-dateutil    # 日期处理
openai            # OpenAI API (可选)
anthropic         # Claude API (推荐)
googletrans       # Google翻译 (备用)
deep-translator    # DeepL翻译
jinja2            # 模板引擎
pydantic          # 数据验证
loguru            # 日志管理
schedule          # 任务调度
```

---

### 1.3 数据结构设计

#### 新闻源配置文件 (`sources.yaml`)

```yaml
# 国内媒体
domestic_sources:
  - name: "新华社"
    url: "http://www.xinhuanet.com/fortune/"
    rss: "http://www.xinhuanet.com/fortune/news_fortune.xml"
    language: "zh"
    category: "domestic"
    priority: 1
    enabled: true

  - name: "财新网"
    url: "https://finance.caixin.com/"
    rss: "https://finance.caixin.com/rss/"
    language: "zh"
    category: "domestic"
    priority: 2
    enabled: true

# 国际媒体
international_sources:
  - name: "Bloomberg"
    url: "https://www.bloomberg.com/markets"
    rss: "https://feeds.bloomberg.com/markets/news.rss"
    language: "en"
    category: "us_europe"
    priority: 1
    enabled: true
    translate: true  # 需要翻译

  - name: "Reuters"
    url: "https://www.reuters.com/finance"
    rss: "https://www.reutersagency.com/feed/"
    language: "en"
    category: "us_europe"
    priority: 2
    enabled: true
    translate: true

  - name: "Nikkei Asia"
    url: "https://asia.nikkei.com/"
    language: "en"
    category: "asia_pacific"
    priority: 1
    enabled: true
    translate: true
```

#### 新闻数据模型

```python
from pydantic import BaseModel, HttpUrl
from datetime import datetime
from typing import Optional, List

class NewsArticle(BaseModel):
    """新闻文章数据模型"""

    # 基础信息
    id: str                          # 唯一标识
    title: str                       # 标题（中文）
    title_original: Optional[str]     # 原始标题（如果是英文）
    content: str                     # 摘要内容（中文，100-150字）
    content_original: Optional[str]   # 原始内容（如果是英文）

    # 来源信息
    source: str                      # 媒体名称（中文）
    source_original: str             # 原始媒体名称
    url: HttpUrl                     # 文章链接
    url_original: Optional[HttpUrl]  # 原始链接（如果是英文）

    # 分类信息
    category: str                    # 板块: domestic/asia_pacific/us_europe
    tags: List[str]                  # 标签: ["货币政策", "日本央行"]

    # 时间信息
    publish_time: datetime           # 发布时间
    crawl_time: datetime             # 抓取时间

    # AI评论
    ai_comment: Optional[str]        # AI锐评

    # 翻译信息
    translated: bool = False         # 是否经过翻译
    translation_method: str = ""     # 翻译方式: "claude" / "deepl" / "google"

    class Config:
        json_encoders = {
            datetime: lambda v: v.isoformat()
        }
```

---

### 1.4 翻译流程设计

#### 翻译策略优先级

```
1. Claude API (推荐) ⭐⭐⭐⭐⭐
   - 优点: 理解金融语境，翻译质量高
   - 缺点: 需要付费
   - 适用: 标题、摘要的精准翻译

2. DeepL API ⭐⭐⭐⭐
   - 优点: 专业翻译，速度快
   - 缺点: 不理解金融黑话
   - 适用: 快速批量翻译

3. Google Translate (免费) ⭐⭐⭐
   - 优点: 免费、易用
   - 缺点: 金融专业度不够
   - 适用: 测试、备份方案
```

#### 翻译质量保证

```python
# 翻译后的处理流程
def translate_article(article: NewsArticle) -> NewsArticle:
    """
    翻译文章并保留原文信息
    """

    # 1. 翻译标题
    title_zh = translate_with_claude(
        article.title_original,
        context="financial_news_title"
    )

    # 2. 翻译摘要（提取关键信息后翻译）
    content_en = extract_summary(article.content_original)
    content_zh = translate_with_claude(
        content_en,
        context="financial_news_summary",
        max_length=150  # 限制字数
    )

    # 3. 保留原文信息
    article.title = title_zh
    article.title_original = article.title_original  # 保留原文
    article.content = content_zh
    article.content_original = content_en  # 保留原文
    article.translated = True
    article.translation_method = "claude"

    return article
```

#### Claude翻译Prompt

```python
TRANSLATE_PROMPT = """
你是一位专业的金融新闻翻译。请将以下英文金融新闻翻译成地道的中文。

## 要求：
1. 保持金融专业性，准确翻译金融术语
2. 符合中文新闻表达习惯
3. 标题简洁有力（不超过30字）
4. 摘要控制在100-150字
5. 保留关键数据、机构名称、人名

## 常用术语对照：
- Central Bank: 央行
- Interest Rate: 利率
- Inflation: 通胀/通货膨胀
- GDP: 国内生产总值
- Treasury Yield: 国债收益率
- Federal Reserve: 美联储

## 原文：
{text}

## 翻译：
"""
```

---

### 1.5 AI锐评生成

#### Claude API配置

```python
import anthropic

client = anthropic.Anthropic(api_key="your-api-key")

def generate_ai_comment(article: NewsArticle) -> str:
    """
    生成AI专业评论
    """

    prompt = f"""
你是一位资深金融分析师，请为以下新闻撰写一句专业评论（30-50字）。

## 新闻标题：
{article.title}

## 新闻摘要：
{article.content}

## 所属板块：
{article.category}

## 评论要求：
1. 使用金融专业术语
2. 揭示背后的逻辑和影响
3. 提供前瞻性判断
4. 一句话，30-50字

## 示例：
输入："日本央行维持利率不变"
输出："按兵不动符合市场预期，但上调通胀预期暗示日央行对可持续通胀回归的信心增强，下半年加息预期升温将支撑日元汇率。"

## 请撰写评论：
"""

    response = client.messages.create(
        model="claude-3-5-sonnet-20241022",
        max_tokens=150,
        messages=[{
            "role": "user",
            "content": prompt
        }]
    )

    return response.content[0].text.strip()
```

#### 成本估算

- Claude Sonnet 3.5: $3/百万输入token + $15/百万输出token
- 每条新闻评论: ~500 token
- 每日15条: ~7,500 token
- **每日成本**: ~$0.11 (约0.8元人民币)
- **每月成本**: ~$3.3 (约24元人民币)

---

### 1.6 抓取调度系统

#### 每日执行时间表

```python
import schedule
import time

def job_fetch_news():
    """主任务：抓取每日新闻"""
    print(f"[{datetime.now()}] 开始抓取新闻...")

    # 1. 抓取国内新闻
    domestic_news = fetch_category("domestic")

    # 2. 抓取亚太新闻
    asia_news = fetch_category("asia_pacific")

    # 3. 抓取美欧新闻
    useu_news = fetch_category("us_europe")

    # 4. 筛选和排序
    selected_news = select_top_news(
        domestic_news,
        asia_news,
        useu_news
    )

    # 5. 生成AI评论
    for article in selected_news:
        if not article.ai_comment:
            article.ai_comment = generate_ai_comment(article)

    # 6. 生成HTML
    generate_html(selected_news)

    print(f"[{datetime.now()}] 新闻抓取完成！")

# 调度：每天上午10:00执行
schedule.every().day.at("10:00").do(job_fetch_news)

# 测试：立即执行
# schedule.run_all()

while True:
    schedule.run_pending()
    time.sleep(60)
```

#### 任务分解

```python
def fetch_category(category: str) -> List[NewsArticle]:
    """
    抓取指定板块的新闻

    Args:
        category: domestic / asia_pacific / us_europe

    Returns:
        该板块的所有候选新闻（筛选前）
    """

    # 1. 确定该板块的新闻源
    sources = get_sources_for_category(category)

    # 2. 并发抓取
    articles = []
    with ThreadPoolExecutor(max_workers=5) as executor:
        futures = [
            executor.submit(fetch_source, source)
            for source in sources
        ]
        for future in as_completed(futures):
            articles.extend(future.result())

    # 3. 去重
    articles = deduplicate(articles)

    # 4. 翻译（如果是英文）
    articles = [translate_article(a) for a in articles if a.language == "en"]

    return articles

def select_top_news(
    domestic: List[NewsArticle],
    asia: List[NewsArticle],
    useu: List[NewsArticle]
) -> List[NewsArticle]:
    """
    从候选新闻中筛选最重要的新闻

    选择标准：
    1. 时间优先：最新24小时
    2. 来源优先：权威媒体优先
    3. 关键词匹配：政策、央行、利率等
    4. 人工筛选（可选）
    """

    def score_article(article: NewsArticle) -> float:
        """新闻重要度评分"""
        score = 0

        # 时间权重（越新越高）
        hours_ago = (datetime.now() - article.publish_time).total_seconds() / 3600
        score += max(0, 24 - hours_ago) * 0.5

        # 来源权重
        source_priority = {
            "新华社": 10, "Bloomberg": 10,
            "财新网": 8, "Reuters": 8,
            "21世纪经济": 7, "WSJ": 7
        }
        score += source_priority.get(article.source, 5)

        # 关键词权重
        keywords = ["央行", "利率", "政策", "GDP", "通胀",
                   "Federal Reserve", "Central Bank", "Interest Rate"]
        if any(kw in article.title for kw in keywords):
            score += 5

        return score

    # 对每个板块筛选TOP 5
    selected = []

    # 国内TOP 5
    domestic_sorted = sorted(domestic, key=score_article, reverse=True)
    selected.extend(domestic_sorted[:5])

    # 亚太TOP 5
    asia_sorted = sorted(asia, key=score_article, reverse=True)
    selected.extend(asia_sorted[:5])

    # 美欧TOP 5
    useu_sorted = sorted(useu, key=score_article, reverse=True)
    selected.extend(useu_sorted[:5])

    return selected
```

---

### 1.7 HTML生成模块

#### Jinja2模板

```html
<!-- templates/news_card.html -->
<article class="news-card {% if featured %}featured{% endif %}">
    <div class="card-header">
        <span class="card-id">{{ article.id }}</span>
        <span class="card-time">{{ article.publish_time|date('YYYY.MM.DD') }}</span>
    </div>

    <h3 class="news-title">{{ article.title }}</h3>

    {% if article.title_original %}
    <p class="news-title-original" style="font-size: 12px; color: #94a3b8;">
        原标题: {{ article.title_original }}
    </p>
    {% endif %}

    <p class="news-excerpt">{{ article.content }}</p>

    <div class="ai-comment">
        <p class="ai-comment-text">{{ article.ai_comment }}</p>
    </div>

    <div class="news-footer">
        <a href="{{ article.url }}" target="_blank" rel="noopener noreferrer" class="news-source">
            {{ article.source }}
            {% if article.source_original %}
            / {{ article.source_original }}
            {% endif %}
        </a>
        <div class="news-meta">
            {% for tag in article.tags %}
            <span class="meta-item">{{ tag }}</span>
            {% endfor %}
        </div>
    </div>
</article>
```

#### 生成函数

```python
from jinja2 import Environment, FileSystemLoader

def generate_html(articles: List[NewsArticle], output_path: str):
    """
    生成最终HTML文件
    """

    # 1. 加载模板
    env = Environment(loader=FileSystemLoader('templates'))
    template = env.get_template('daily_news.html')

    # 2. 按板块分组
    domestic = [a for a in articles if a.category == "domestic"]
    asia = [a for a in articles if a.category == "asia_pacific"]
    useu = [a for a in articles if a.category == "us_europe"]

    # 3. 渲染
    html = template.render(
        date=datetime.now().strftime("%Y-%m-%d"),
        domestic_news=domestic,
        asia_news=asia,
        useu_news=news,
        total_articles=len(articles)
    )

    # 4. 保存
    with open(output_path, 'w', encoding='utf-8') as f:
        f.write(html)

    print(f"HTML已生成: {output_path}")
```

---

### 1.8 质量控制

#### 自动检查清单

```python
def quality_check(article: NewsArticle) -> bool:
    """新闻质量检查"""

    checks = {
        "title长度": 10 <= len(article.title) <= 50,
        "摘要长度": 100 <= len(article.content) <= 200,
        "有来源": bool(article.source),
        "有链接": bool(article.url),
        "链接有效": check_url_alive(article.url),
        "有AI评论": bool(article.ai_comment),
        "评论长度": 20 <= len(article.ai_comment) <= 80,
        "非重复": not is_duplicate(article),
    }

    passed = all(checks.values())

    if not passed:
        print(f"质量检查失败: {article.title}")
        for name, result in checks.items():
            if not result:
                print(f"  ✗ {name}")

    return passed
```

---

## 📋 待确认事项（Phase 1完成后）

### Phase 2: 更新工作流优化（优先级：P1）
- [ ] 每日更新时间点确认
- [ ] 是否需要通知机制（邮件/Telegram）
- [ ] 错误处理和重试策略
- [ ] 日志记录和监控

### Phase 3: 历史归档系统（优先级：P2）
- [ ] 数据库选择（SQLite / PostgreSQL）
- [ ] 搜索功能实现
- [ ] 归档文件管理
- [ ] 数据备份策略

### Phase 4: 交互功能增强（优先级：P3）
- [ ] 深色模式切换
- [ ] 收藏功能
- [ ] 社交分享
- [ ] PDF导出

### Phase 5: 分发渠道（优先级：P4）
- [ ] 邮件订阅系统
- [ ] 微信公众号同步
- [ ] Notion集成

---

## 🚀 下一步行动

### 立即开始（今天）
1. ✅ 创建Python虚拟环境
2. ✅ 安装依赖库
3. ✅ 创建项目目录结构
4. ✅ 配置新闻源YAML文件

### 本周完成
1. [ ] 实现单个新闻源抓取器（以新华社为测试）
2. [ ] 实现翻译模块（Claude API）
3. [ ] 实现AI评论生成（Claude API）
4. [ ] 实现HTML生成模板

### 下周完成
1. [ ] 完成所有主流新闻源配置
2. [ ] 实现调度系统
3. [ ] 测试完整流程
4. [ ] 部署到服务器（如需）

---

## 📊 预期成果

### 自动化前后对比

| 项目 | 手动更新 | 自动化后 |
|------|---------|---------|
| 每日耗时 | 2-3小时 | 5分钟（审核） |
| 新闻来源 | 3-5个 | 15+个 |
| 国际新闻 | ❌ 无 | ✅ 自动翻译 |
| 更新频率 | 不稳定 | 每日准时 |
| AI评论 | 手动撰写 | 自动生成 |
| 历史记录 | ❌ 无 | ✅ 数据库 |

### 成本估算

**月度成本**:
- Claude API: $3.3 (AI评论)
- DeepL: $0 (免费额度够用)
- 服务器: $0 (本地运行) 或 $5-10 (云服务器)
- **总计**: $3-13 / 月 (约20-100元)

---

## 📝 备注

### 已确认的需求
✅ 新闻源包括国内外主流媒体
✅ 英文内容翻译成中文
✅ 保留英文原始出处
✅ 使用Claude API生成AI评论
✅ 每日自动更新

### 待决策
- [ ] 服务器部署方式（本地/云端）
- [ ] 是否需要人工审核环节
- [ ] Claude API账号配置
- [ ] 错误通知方式

---

**文档版本**: v1.0
**最后更新**: 2026年1月25日
