name: 重新生成HTML

on:
  # 手动触发
  workflow_dispatch:
    inputs:
      days:
        description: '读取最近N天的新闻'
        required: false
        default: '7'
        type: choice
        options:
          - 7
          - 14
          - 30

jobs:
  regenerate:
    runs-on: ubuntu-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: 安装依赖
      run: |
        cd news_bot
        pip install -r requirements.txt

    - name: 缓存数据库文件
      uses: actions/cache@v4
      with:
        path: news_bot/data/news.db
        # key 决定了本次运行产生的缓存叫什么名字
        key: news-db-${{ runner.os }}-${{ github.run_id }}
        # restore-keys 决定了如果 key 没命中，去捡哪一个旧缓存来用
        restore-keys: |
          news-db-${{ runner.os }}-
          news-db-

    - name: 设置环境变量
      run: |
        cd news_bot
        # 创建.env文件
        cat > .env << EOF
        ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}
        ANTHROPIC_BASE_URL=${{ secrets.ANTHROPIC_BASE_URL }}
        CLAUDE_MODEL=claude-3-5-sonnet-20241022
        MAX_AGE_HOURS=48
        TOP_NEWS_PER_CATEGORY=5
        EOF

    - name: 重新生成HTML
      run: |
        cd news_bot
        # 如果是手动触发使用用户指定的天数，否则默认7天
        DAYS=${{ inputs.days || '7' }}
        echo "🔄 开始重新生成HTML（最近 $DAYS 天）"
        python main.py --html-only --days $DAYS

    - name: 安装 Vercel CLI
      if: success()
      run: npm install --global vercel@latest

    - name: 部署到 Vercel
      if: success()
      run: |
        npm install --global vercel@latest

        if [ "${{ github.ref }}" = "refs/heads/main" ]; then
          echo "🚀 正在部署到生产环境 (Production)..."
          vercel deploy ./public --prod --name daily-news-bot --token=${{ secrets.VERCEL_TOKEN }} --yes
        else
          echo "🧪 正在部署到测试预览环境 (Preview)..."
           # 不带 --prod 参数，Vercel 会生成一个临时预览链接
          vercel deploy ./public --name daily-news-bot --token=${{ secrets.VERCEL_TOKEN }} --yes
        fi

    - name: 部署完成提示
      if: success()
      run: |
        echo "✅ HTML 文件已直接部署到 Vercel"
        echo "✅ 未提交到 GitHub 仓库"
        echo "✅ 数据库已缓存到 GitHub Actions"
