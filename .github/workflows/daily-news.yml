name: 每日财经新闻生成

on:
  # 每天北京时间上午9点（UTC 1点）运行
  schedule:
    - cron: '0 1 * * *'

  # 允许手动触发
  workflow_dispatch:

  # 可选：推送到main分支时也运行（测试用）
  # push:
  #   branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 获取完整历史，用于推送到main分支

    - name: 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: 安装依赖
      run: |
        cd news_bot
        pip install -r requirements.txt

    - name: 设置环境变量
      run: |
        cd news_bot
        # 创建.env文件
        cat > .env << EOF
        ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}
        ANTHROPIC_BASE_URL=${{ secrets.ANTHROPIC_BASE_URL }}
        CLAUDE_MODEL=claude-3-5-sonnet-20241022
        MAX_AGE_HOURS=48
        TOP_NEWS_PER_CATEGORY=5
        EOF

    - name: 运行新闻抓取
      run: |
        cd news_bot
        python main.py

    - name: 配置Git用户
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'

    - name: 提交并推送HTML文件
      run: |
        # 检查是否有新文件
        if [ -f "public/$(date +%Y-%m-%d).html" ]; then
          git add public/*.html
          git diff --staged --quiet || git commit -m "📰 每日新闻更新 - $(date +'%Y-%m-%d')"
          git push origin main
        else
          echo "警告：未生成今日HTML文件"
          exit 1
        fi

    - name: 触发Vercel部署
      if: success()
      run: |
        echo "✓ HTML文件已推送到main分支"
        echo "✓ Vercel将自动检测并部署"
        echo "✓ 访问你的Vercel仪表板查看部署状态"
