name: 每日财经新闻生成

on:
  schedule:
    # 北京时间 07:05（UTC 23:05）
    - cron: '5 23 * * *'
    # 北京时间 12:05（UTC 04:05）
    #- cron: '5 4 * * *'
    # 北京时间 18:05（UTC 10:05）
    #- cron: '5 10 * * *'
  # 允许手动触发
  workflow_dispatch:
    inputs:
      html_only:
        description: '仅重新生成HTML(不抓取新数据)'
        type: boolean
        default: false
      days:
        description: '读取最近N天的新闻'
        type: choice
        default: '30'
        options:
          - 30
          - 14
          - 7
          - 3
          - 1
      test_mode:
        description: '测试模式(强制部署到 Preview)'
        type: boolean
        default: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
    steps:
      - name: 检出代码
        uses: actions/checkout@v4      

      - name: Time sanity check
        run: |
          echo "Event: $GITHUB_EVENT_NAME"
          echo "Runner now (default date):"; date
          echo "UTC now:"; date -u
          echo "TZ env: $TZ"
          python - <<'PY'
          import os, time
          from datetime import datetime, timezone
          from zoneinfo import ZoneInfo
          print("python now():", datetime.now())
          print("python now(utc):", datetime.now(timezone.utc))
          print("python now(Asia/Shanghai):", datetime.now(ZoneInfo("Asia/Shanghai")))
          print("time.tzname:", time.tzname)
          print("TZ env:", os.getenv("TZ"))
          PY

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安装依赖
        run: |
          cd news_bot
          pip install -r requirements.txt

      - name: 缓存数据库文件
        uses: actions/cache@v4
        with:
          path: news_bot/data/news.db
          # ✅ 使用 Commit SHA 作为缓存 key，每次运行都有唯一的 key
          # restore-keys 会恢复最近一次的缓存，确保数据持续累积
          key: news-db-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            news-db-${{ runner.os }}-
            news-db-

      - name: Debug DB file info
        run: |
          ls -lah news_bot/data || true
          ls -lah news_bot/data/news.db || true
          du -h news_bot/data/news.db || true

      - name: 设置环境变量
        run: |
          cd news_bot
          # 创建 .env 文件
          cat > .env << EOF
          ANTHROPIC_API_KEY=${{ secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL=${{ secrets.ANTHROPIC_BASE_URL }}
          CLAUDE_MODEL=claude-3-5-sonnet-20241022
          MAX_AGE_HOURS=48
          TOP_NEWS_PER_CATEGORY=5
          TEST_MODE=${{ inputs.test_mode }}
          EOF

      - name: 运行新闻抓取
        run: |
          cd news_bot
          if [ "${{ inputs.html_only }}" = "true" ]; then
            echo "🔄 仅重新生成HTML，不抓取新数据"
            python main.py --html-only --days "${{ inputs.days }}"
          elif [ "${{ inputs.test_mode }}" = "true" ]; then
            echo "🧪 测试模式"
            python main.py --test
          else
            echo "📰 正常模式：抓取新闻并生成HTML"
            python main.py
          fi

      # ✅ 修复：先安装 Vercel CLI（否则会出现 vercel: command not found）
      - name: 安装 Vercel CLI
        run: |
          npm install -g vercel@latest
          vercel --version

      - name: 部署到 Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          # 在 CI 中用 secrets 指定的 Org/Project 绑定，避免默认用目录名（比如 public）创建新项目
          if [ "${{ inputs.test_mode }}" = "true" ]; then
            echo "🧪 测试模式：强制部署到测试预览环境 (Preview)..."
            echo "🔗 绑定到 Vercel Project (preview)..."
            vercel pull --yes --environment=preview --token="$VERCEL_TOKEN"
            echo "🧪 正在部署到测试预览环境 (Preview)..."
            # 不带 --prod 参数，Vercel 会生成一个临时预览链接
            vercel deploy ./public --token="$VERCEL_TOKEN" --yes
          elif [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "🔗 绑定到 Vercel Project (production)..."
            vercel pull --yes --environment=production --token="$VERCEL_TOKEN"
            echo "🚀 正在部署到生产环境 (Production)..."
            vercel deploy ./public --prod --token="$VERCEL_TOKEN" --yes
          else
            echo "🔗 绑定到 Vercel Project (preview)..."
            vercel pull --yes --environment=preview --token="$VERCEL_TOKEN"
            echo "🧪 正在部署到测试预览环境 (Preview)..."
            # 不带 --prod 参数，Vercel 会生成一个临时预览链接
            vercel deploy ./public --token="$VERCEL_TOKEN" --yes
          fi

      - name: 部署完成提示
        run: |
          echo "✅ HTML 文件已直接部署到 Vercel"
          echo "✅ 未提交到 GitHub 仓库"
          echo "✅ 数据库已缓存到 GitHub Actions"
